import streamlit as st 
import pandas as pd 

# defining types
from pandas import DataFrame
from src.graph import Vertex
from typing import List, Set, Dict, Any

def show_in_streamlit_generated_vertexs_like_a_dataframe ( vertexs: List[ Vertex ] ) -> None:
  # transform vertex in ( name, type, [ metadatas ] ) => ( name, type, like-movies, like-music )

  pass

